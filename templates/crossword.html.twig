<div{{ attributes.addClass('crossword').addClass('show-references') }}>

  <div class="crossword-header">
    {% if content.title is not empty or content.author is not empty or content.notepad is not empty %}
      <div class="crossword-details">
        {{ content.title }}
        {{ content.author }}
        {{ content.notepad }}
      </div>
    {% endif %}
    {{ content.controls }}
  </div>

  <div id="active-clues" class="active-clues">
  {# This gets populated by js #}
  </div>

  <div class="crossword-puzzle clearfix">
    {{ content.grid }}
    {{ content.across }}
    {{ content.down }}
  </div>

</div>

Crossword Media
===============
This module provides a simple extension of the File media source that allows
the crossword field type to be used as a media source.

It would be neat if a thumbnail representing the crossword were generated to be
used as the thumbnail, but that doesn't happen yet.
